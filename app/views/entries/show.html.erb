<% content_for :middle do %>
  <% entry_id = request.env['PATH_INFO'].split("/").last %>
  <!-- <div id="note-button">Leave a Note</div> -->
  <div id="star-space">
  	<% 10.times do |i| %>
  		<div class="star-wrapper<%= " voted" if @voted_value == 10 - i %>"><%= link_to "<div class=\"star\" data-value=\"#{10 - i}\"></div>".html_safe, cast_path(:value => 10 - i, :entry_id => entry_id), :method => "put" %></div>
  	<% end %>
  </div>
<% end %>

<iframe src="<%= @entry.demo_url %>" id="demo-frame"></iframe>